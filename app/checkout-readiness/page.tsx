import type { Metadata } from "next";
import { ContentPage, ContentSection } from "@/components/storefront/ContentPage";
export const metadata: Metadata = { title: "Checkout Readiness", robots: { index: false, follow: true } };
const gates = ["Exact SKU images, dimensions, weight and capacity", "Composition, lining, compatibility and care approval", "GST/HSN treatment and invoice configuration", "Packed weight, pincode serviceability and dispatch SLA", "Published return eligibility and grievance channel", "Server-authoritative stock, totals and coupon validation", "Razorpay order/signature/webhook verification", "Shiprocket shipment, tracking and reverse-pickup testing"];
export default function CheckoutReadinessPage() { return <ContentPage eyebrow="Commerce readiness" title="Checkout opens when every promise is supportable." introduction="The bag is functional for selection planning. Payment remains deliberately unavailable until the product, legal, tax, inventory and fulfilment gates below are evidenced end to end."><ContentSection title="Activation checklist"><ol className="readiness-list">{gates.map((gate, index) => <li key={gate}><span>{String(index + 1).padStart(2, "0")}</span>{gate}<strong>Pending evidence</strong></li>)}</ol></ContentSection><aside className="content-notice"><strong>Why this matters</strong><p>A visually complete checkout without server-verified amounts, inventory and policy data creates customer, financial and legal risk. The production architecture and interfaces are defined; activation requires the real operating inputs.</p></aside></ContentPage>; }
